#include <stdio.h>

/**
* main - Print all single digit numbers
*
* Return: Always 0 (Success)
*/
int main(void)
{
	int d;

	for (d = 0; d < 10; d++)
	printf("%i", d);
	putchar('\n');

	return (0);
}
